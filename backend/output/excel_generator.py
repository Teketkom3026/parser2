"""Генерация итогового Excel- файла.""" from openpyxl import Workbook from openpyxl.styles import Font, Alignment, PatternFill, Border, Side from openpyxl.utils import get_column_letter from backend.utils.logger import get_logger logger = get_logger("excel_generator") HEADERS = [ "Название компании", # A "Сайт (домен)", # B "Общий email компании", # C "Общий телефон компании", # D "ФИО сотрудника", # E "Должность (как на сайте)", # F "Должность (нормализованная)", # G "Категория роли", # H "Личный email", # I "Личный телефон", # J "ИНН", # K "КПП", # L "Соцсети", # M "URL страницы-источника", # N "Язык страницы", # O "Дата сканирования", # P "Статус обработки", # Q
    "Комментарий", # R ] FIELD_MAP = [ "company_name", "site_url", "company_email", "company_phone", "person_name", "position_raw", "position_norm", "role_category", "person_email", "person_phone", "inn", "kpp", "social_links", "page_url", "page_language", "scan_date", "status", "comment", ] def generate_excel(contacts: list[dict], output_path: str) -> str: wb = Workbook() ws = wb.active ws.title = "Контакты" # ── Стили заголовка ── header_font = Font(bold=True, color="FFFFFF", size=11) header_fill = PatternFill(start_color="3F51B5", end_color="3F51B5", fill_type="solid") header_align = Alignment(horizontal="center", vertical="center", wrap_text=True) thin_border = Border( left=Side(style="thin"), right=Side(style="thin"), top=Side(style="thin"), bottom=Side(style="thin"), ) # ── Заголовки ── for col_idx, header in enumerate(HEADERS, 1): cell = ws.cell(row=1, column=col_idx, value=header) cell.font = header_font cell.fill = header_fill cell.alignment = header_align cell.border = thin_border # ── Данные ── row_fill_even = PatternFill(start_color="F5F5F5", end_color="F5F5F5", fill_type="solid") for row_idx, contact in enumerate(contacts, 2): for col_idx, field_name in enumerate(FIELD_MAP, 1): value = contact.get(field_name, "") or "" cell = ws.cell(row=row_idx, column=col_idx, value=str(value)) cell.border = thin_border cell.alignment = Alignment(vertical="top", wrap_text=True) if row_idx % 2 == 0: cell.fill = row_fill_even # ── Ширины колонок ── col_widths = [25, 20, 25, 20, 28, 30, 30, 20, 28, 20, 14, 12, 30, 40, 8, 14, 10, 25]
    for col_idx, width in enumerate(col_widths, 1): ws.column_dimensions[get_column_letter(col_idx)].width = width # ── Фиксация заголовка ── ws.freeze_panes = "A2" # ── Автофильтр ── ws.auto_filter.ref = f"A1:R{len(contacts) + 1}" # ── Лист-сводка ── ws2 = wb.create_sheet(" Сводка") ws2.cell(row=1, column=1, value=" Метрика"). font = Font(bold=True) ws2.cell(row=1, column=2, value=" Значение"). font = Font(bold=True) total = len(contacts) with_name = sum(1 for c in contacts if c.get("person_name")) with_email = sum(1 for c in contacts if c.get("person_email")) with_position = sum(1 for c in contacts if c.get("position_norm")) unique_companies = len(set(c.get("site_url", "") for c in contacts)) summary_data = [ ("Всего записей", total), ("С ФИО сотрудника", with_name), ("С личным email", with_email), ("С должностью", with_position), ("Уникальных компаний", unique_companies), ] for i, (label, val) in enumerate(summary_data, 2): ws2.cell(row=i, column=1, value=label) ws2.cell(row=i, column=2, value=val) ws2.column_dimensions["A"].width = 25 ws2.column_dimensions["B"].width = 15 wb.save(output_path) logger.info("excel_generated", path=output_path, rows=total) return output_path
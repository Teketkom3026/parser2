"""Тесты нормализации должностей.""" from backend.extractor.normalizer import ( normalize_position, classify_role, filter_by_positions, ) def test_normalize_exact():
    assert normalize_position(" генеральный директор") == "Генеральный директор" assert normalize_position("ceo") == "CEO" assert normalize_position(" главбух") == "Главный бухгалтер" def test_normalize_fuzzy(): result = normalize_position(" ген. директор") assert "директор" in result.lower() or "Генеральный" in result def test_classify_role(): assert classify_role(" Генеральный директор") == "Топ-менеджмент" assert classify_role("CEO") == "Топ-менеджмент" assert classify_role(" Начальник отдела") == "Средний менеджмент" assert classify_role(" Программист") == "Специалист" def test_filter_by_positions(): persons = [ {"position_raw": "Генеральный директор", "position_norm": "Генеральный директор"}, {"position_raw": "Программист", "position_norm": "Программист"}, {"position_raw": "CEO", "position_norm": "CEO"}, ] filtered = filter_by_positions(persons, ["директор", "CEO"]) assert len(filtered) == 2